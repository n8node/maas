map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

upstream mn_backend {
    server backend:8080;
    keepalive 32;
}

upstream mn_frontend {
    server frontend:3000;
    keepalive 32;
}

upstream mn_wordpress {
    server wordpress:80;
    keepalive 8;
}

server {
    listen 80;
    listen [::]:80;
    server_name localhost mnemoniqa.com www.mnemoniqa.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /api/ {
        proxy_pass http://mn_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
        client_max_body_size 50M;
    }

    location = /health {
        proxy_pass http://mn_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    location /_next/ {
        proxy_pass http://mn_frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location /dashboard {
        proxy_pass http://mn_frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
    }

    location /wp-admin/ {
        proxy_pass http://mn_wordpress;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /wp-content/ {
        proxy_pass http://mn_wordpress;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /wp-includes/ {
        proxy_pass http://mn_wordpress;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /wp-json/ {
        proxy_pass http://mn_wordpress;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location = /wp-login.php {
        proxy_pass http://mn_wordpress;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://mn_wordpress;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# HTTPS: раскомментировать после размещения fullchain.pem и privkey.pem в nginx/ssl/
# и добавить в этот server редирект return 301 https://$host$request_uri;
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name mnemoniqa.com www.mnemoniqa.com;
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     include snippets duplicated locations …
# }
