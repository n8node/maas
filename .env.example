# Copy to .env and fill secrets. Never commit .env

ENVIRONMENT=development

# Production domain (Let's Encrypt / nginx server_name)
DOMAIN=mnemoniqa.com

# Email for Let's Encrypt registration and expiry notices (required by ACME)
CERTBOT_EMAIL=erman.ai@yandex.ru

# Database, JWT, API keys — see memory-service-deploy.mdc when backend exists
# POSTGRES_PASSWORD=
# JWT_SECRET=
# OPENROUTER_API_KEY=
